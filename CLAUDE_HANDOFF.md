# 장기 프로젝트 — Claude Code 인수인계 문서

작성일: 2026-08-08  
프로젝트 경로: `/Users/minseokjeong/Documents/Codex/2026-08-02/project-organ`  
프로덕션: https://project-organ.vercel.app  
에셋 보드: https://project-organ.vercel.app/assets

## 0. 가장 먼저 확인할 것

현재 최신 구현은 Vercel 프로덕션에 배포되어 있지만 Git에는 아직 커밋되지 않았다.

- 기준 커밋: `c64b29a Pause mobile portrait gameplay and hydrate saves safely`
- `app/organ-game.tsx`, 타입, CSS, 오디오, 신규 이미지와 `/assets` 페이지가 working tree에 남아 있다.
- 기존 변경을 되돌리거나 덮어쓰지 말 것.
- 먼저 `git status --short`와 `git diff --stat`을 확인한다.
- `tmp/`와 `tsconfig.tsbuildinfo`는 결과물이 아니라 로컬 생성 파일이다. 커밋 전 정리하거나 `.gitignore`에 추가한다.
- 현재 상태를 검증한 뒤 별도 커밋으로 보존하고 `main`에 push하는 작업이 최우선이다.

현재 프로덕션 배포 ID:

- `dpl_EEkAHs6xJrdhAwWRy2KWmffiQuiq`
- production alias: `https://project-organ.vercel.app`

## 1. 프로젝트 배경

NHN NAN 2026 게임 대회를 준비하는 2인 팀 프로젝트다.

Game Lab을 통해 여러 프로토타입을 공개하고 실제 행동 데이터와 평가를 수집했다. 그 결과 현재 집중 개발 대상으로 **장기 프로젝트**를 선택했다.

관련 외부 프로젝트:

- Game Lab production: https://game-lab-nhn-2026.vercel.app
- GitHub organization: https://github.com/project-organ0
- Game Lab은 7개 프로토타입의 유입, 게임 시작, 플레이 시간, 평가와 최종 투표를 수집한다.
- 데이터 저장은 Neon Postgres를 사용한다.
- 일반 사용자는 결과 화면에 접근하지 못하게 하고, 팀만 대시보드와 이벤트 로그를 확인하는 방향이다.
- 장기 프로젝트는 Game Lab 데이터에서 플레이 지속시간, 재미, Want More가 상대적으로 강해 최종 집중 개발 대상으로 선택됐다.

핵심 제품 가설은 다음과 같다.

> 장기는 단순 스탯 아이템이 아니라 플레이 스타일을 결정하는 빌드 언어다.

게임은 탑다운 생존 로그라이크이며, 학교 → 회사 → 아파트 → 병원이라는 생애 구간을 통과해 마지막 적 `노화`와 싸운다. 주인공은 같은 흰머리 캐릭터를 유지하고, 어떤 장기를 각성했는지에 따라 전투 방식과 이펙트가 바뀐다.

대회 범위의 핵심 장기는 세 개다.

- 심장 → 격투가: 적에게 얼마나 가까이 붙을 것인가
- 뇌 → 에너지술사: 적과 어떤 거리를 유지할 것인가
- 간 → 독술사: 적을 어디로 유도할 것인가

조작은 모든 직업에서 이동 중심으로 유지한다. 데스크톱은 WASD, 모바일은 가상 스틱이며 SPACE/우측 액션 버튼으로 직업 액션을 사용한다.

## 2. 기술 구조

- Next.js 16 + React 19 + TypeScript
- 실제 로컬 빌드는 `vinext` 기반
- Canvas 2D 게임
- 별도 게임 엔진 없음
- 상태와 전투 루프 대부분이 `app/organ-game.tsx`에 집중되어 있음
- 배포: Vercel 프로젝트 `project-organ`
- 데이터베이스 없음
- Game Lab 연동: `window.opener.postMessage`

실행 명령:

```bash
npm install
npm run dev
npm run build
```

프로덕션 배포:

```bash
npx vercel --prod --yes --scope minseoks-projects-328b4396
```

Node 요구 버전은 `>=22.13.0`이다.

## 3. 핵심 파일

### 게임

- `app/organ-game.tsx`
  - 게임 상태 생성
  - 카드 데이터와 선택 규칙
  - 플레이 입력
  - 몬스터 생성과 전투 루프
  - Canvas 렌더링
  - HUD, 카드 선택, 메뉴, 결과 화면
  - Game Lab 이벤트 전송
- `app/game/types.ts`
  - `Game`, `Mob`, `Shot`, `ToxicField`, 카드 타입
- `app/game/audio.ts`
  - WebAudio 효과음
  - `/audio/circuit-bloom.ogg` BGM 재생
- `app/game/portals.ts`
  - 장기별 포털 구역
- `app/globals.css`
  - 게임 전체 UI
  - 모바일 가로/세로 대응
  - 카드 등급과 융합 카드 스타일

### 개발용 에셋 보드

- `app/assets/page.tsx`
- `app/assets/assets.css`
- URL: `/assets`
- 현재 사용 중인 V2 이미지와 27장 카드 명세를 한눈에 확인하는 개발 전용 페이지다.

### 주요 에셋

- `public/art/player-forms-v2-clean.png`: 흰머리 주인공 형태
- `public/art/player-vfx.png`: 이동, 대시, 발사, 피격, 장기 각성 VFX
- `public/art/projectiles.png`: 원형 투사체
- `public/art/school-walk.png`
- `public/art/company-walk.png`
- `public/art/apartment-walk.png`
- `public/art/hospital-walk.png`
- `public/art/maps/*.png`: 스테이지 배경
- `public/audio/circuit-bloom.ogg`: Neon Pulse 프로젝트에서 가져온 BGM
- `*-v1.png`: 이전 몬스터 아트 백업. 삭제 여부는 팀 확인 후 결정

## 4. 현재 구현된 게임 시스템

### 장기 성장과 각성

- 심장, 뇌, 간은 Lv.0 → Lv.3으로 성장한다.
- Lv.3에 도달하면 해당 직업으로 각성하거나 보류할 수 있다.
- 각성하면 `mainClass`가 한 판 동안 고정된다.
- 각성 전에는 공용 자동 에너지탄을 사용한다.
- 카드 선택은 마우스/터치 외에 `1/2/3`, `A/D`, `SPACE/Enter`를 지원한다.

### 카드 풀 27장

- 장기 성장 3장
- 심장 전용 4장
- 뇌 전용 4장
- 간 전용 4장
- 융합 6장
- 생활 선택 3장
- 공용 생존 3장

등장 가중치:

- 각성 전: 장기 60 / 생활 20 / 공용 20
- 각성 후: 현재 직업 45 / 장기 25 / 생활 15 / 공용 15
- 메인 직업 확정 후 보조 장기가 Lv.2가 되면, 아직 획득하지 않은 해당 융합 카드가 다음 선택의 한 슬롯에 확정 등장한다.
- 직업 카드는 최대 Lv.3, 융합/생활/공용 카드는 1회성이다.

### 심장 / 격투가

- 근처 적을 자동 근접 공격한다.
- 4타 콤보 구조가 있다.
- 아드레날린: 근거리 공격 속도 상승
- 심박 충격: 4타째 범위 충격파와 밀치기
- 과부하 연타: 동일 몬스터 누적 5타 후 강한 공격
- 혈류 가속: 근거리 처치 후 이동 가속
- SPACE: 전방 돌진 펀치

### 뇌 / 에너지술사

- 플레이어 주변에 실제로 보이는 에너지 코어가 회전한다.
- 기본 코어 2개
- 시냅스 증식: 코어 추가
- 연쇄 사고: 다른 적에게 투사체 연쇄
- 집중 사고: 높은 체력 적 우선, 엘리트/보스 추가 피해
- 사고 폭주: 5킬마다 코어 일제 사격
- SPACE: 근처 적을 향한 코어 집중 사격

### 간 / 독술사

- 이동 거리에 따라 경로에 독 지대를 생성한다.
- 독성 발자국: 더 촘촘하고 오래 유지
- 오염 중첩: 기존 지대를 다시 지나면 최대 3중첩, 범위 증가
- 독성 파열: 중독된 적 사망 시 폭발과 전염
- 농축 독: 독 지대 체류 중 1초마다 중독 중첩
- SPACE: 현재 위치에 큰 독 지대 생성

### 융합 6종

- 심장 + 뇌: 뇌근 동기화 — 콤보 피니시 추적탄
- 심장 + 간: 독성 파이터 — 근접 중독과 피니시 폭발
- 뇌 + 심장: 맥동 코어 — 근접 시 코어 가속
- 뇌 + 간: 신경 독성 — 중독 적 우선과 독 전염
- 간 + 심장: 독성 폭주 — 독 지대 3킬마다 폭발
- 간 + 뇌: 추적 독성 — 적 위치로 독 지대 자동 전파

### 생활 카드

- 밤샘 공부: 연쇄 +1, 공격 주기 감소 / 최대 체력 -15%
- 운동부 입단: 근접 범위와 처치 이동 증가 / 원거리 피해 -10%
- 회식의 제왕: 독 범위와 지속시간 증가 / 회복 효과 -25%

### 공용 생존 카드

- 세포 분열: 40% 체력으로 1회 부활
- 재생 인자: 20킬마다 최대 체력 8% 회복
- 세포막 강화: 8초 무피격 시 최대 체력 15% 보호막

### 모바일

- 세로 화면에서는 회전 안내를 띄우고 게임을 일시 정지한다.
- 가로 화면에서는 가상 스틱과 액션 버튼을 사용한다.
- 844×390 기준 카드 세 장이 한 화면에 들어오도록 별도 compact CSS가 있다.

### BGM과 사운드

- 게임 시작 시 BGM 재생
- 일시정지/화면 회전 시 정지
- 복귀 시 재생
- 효과음은 WebAudio oscillator 방식
- 브라우저 autoplay 정책상 게임 시작 버튼을 누른 뒤 재생된다.

## 5. Game Lab 이벤트 연동

현재 `window.opener`가 있을 때 아래 이벤트를 `postMessage`로 보낸다.

- `game_run_started`
- `game_restarted`
- `game_progress` — 10/30/60/120/180/300초
- `game_stage_reached`
- `game_boss_reached`
- `game_choice_shown`
- `game_choice_selected`
- `game_run_ended`
- `game_completed`

중요:

- 현재 `targetOrigin`은 `"*"`이다.
- 게임을 새 탭/팝업이 아니라 같은 탭으로 열면 `window.opener`가 없어 이벤트가 전송되지 않는다.
- Game Lab 실제 배포 동선에서 이벤트가 Neon DB까지 들어가는지 다시 종단 테스트해야 한다.
- 새 카드 분석을 위해 `choiceId`, `cardLevel`, `mainClass`, `supportLevels`, `fusion` 여부를 이벤트 metadata에 추가하는 것이 좋다.

## 6. 지금까지 확인된 검증 결과

- `npm run build` 성공
- Vercel production build 성공
- 프로덕션 `/` 정상 로드, 오류 overlay 없음
- 로컬에서 게임 시작 → 18초 생활 카드 표시 → 카드 선택 동작 확인
- `/assets`에서 카드 27개, LIVE 27개 표시 확인
- 844×390 모바일 가로 viewport의 카드 배치 확인

주의:

- `npx tsc --noEmit`은 게임 코드가 아니라 기존 `worker/index.ts`의 Cloudflare 전역 타입 `Fetcher`, `D1Database` 미설정 때문에 실패한다.
- Vercel 빌드는 타입 검사를 생략하고 성공한다.
- 완주 단위의 세 직업/여섯 융합 E2E 플레이 테스트는 아직 하지 않았다.

## 7. 남은 작업 — 우선순위 순

### P0. 현재 변경분 보존

1. working tree 전체 검토
2. `tmp/`, `tsconfig.tsbuildinfo` 제외
3. 필요한 신규 에셋을 Git에 추가
4. 현재 상태 커밋
5. `main` push
6. GitHub 연동 Vercel 빌드가 현재 CLI 배포와 같은 결과인지 확인

이 작업을 먼저 하지 않으면 프로덕션과 GitHub 소스가 서로 다른 상태로 남는다.

### P0. 세 직업과 융합 실제 플레이 검증

개발용 빠른 검증 모드를 추가하는 것이 좋다.

예시:

- URL: `/?debug=heart`, `/?debug=brain`, `/?debug=liver`
- 장기 레벨과 카드를 즉시 부여
- 특정 융합을 바로 활성화
- 보스/엘리트 즉시 소환

반드시 확인할 시나리오:

1. 각성 전 공용 탄환
2. 각 장기 Lv.3과 각성/보류
3. 심장 4타, 충격파, 과부하, 돌진
4. 뇌 코어 수 증가, 연쇄, 집중 타깃, 5킬 폭주
5. 간 발자국, 지대 3중첩, 농축, 사망 파열
6. 여섯 융합 각각의 발동
7. 부활, 20킬 회복, 무피격 실드
8. 최종 보스와 결과 화면

### P0. 밸런스 조정

현재 값은 기능 확인용 초깃값이다. 실제 재미와 난이도 튜닝이 필요하다.

특히 확인할 것:

- 표준 난이도에서 첫 선택 전후 사망률
- 첫 각성이 평균 몇 분에 발생하는지
- 각성 전 구간이 너무 길거나 지루하지 않은지
- 심장이 적에게 접근할 보상을 충분히 주는지
- 뇌의 코어 수 증가가 과도한 DPS/화면 혼잡을 만들지 않는지
- 간이 계속 이동만 해도 지나치게 안전하거나 강하지 않은지
- 보조 장기 Lv.2와 융합 카드가 한 판에서 현실적으로 등장하는지
- 카드 선택 중 게임 일시정지가 정확히 유지되는지

추천 목표:

- 첫 각성: 시작 후 약 60~100초
- 첫 융합: 시작 후 약 2~3분
- 직업별 생존 시간과 처치 수가 한쪽으로 20% 이상 치우치지 않게 조정

### P1. 카드 명세와 현재 동작의 세부 일치

27장은 모두 동작하지만 Lv.2/Lv.3의 세부 강화는 일부만 차등 적용되어 있다.

보완이 필요한 예:

- 혈류 가속 Lv.2/Lv.3의 지속시간 또는 속도 차등
- 집중 사고 Lv.2/Lv.3의 보스 보너스 차등
- 사고 폭주 Lv.2/Lv.3의 발동 간격/추가 탄 차등
- 오염 중첩 Lv.2/Lv.3의 중첩 효율 차등
- 농축 독 Lv.2/Lv.3의 중첩 주기 차등
- 심장 과부하는 현재 몬스터별 누적값이 오래 유지된다. “짧은 시간 동안 같은 적 5회”가 되도록 타임아웃 또는 현재 타깃 추적을 추가해야 한다.

### P1. 신규 시스템에 맞춘 메타 메뉴 정리

시작 화면의 영웅 도감과 유전 기록에는 아직 이전 5장기/6케미 모델이 남아 있다.

정리 방향:

- 영웅 도감: 심장/뇌/간 3개 주 직업 중심
- 융합 도감: 현재 6종 융합 카드 중심
- “레벨 5에서 각성” 같은 이전 문구 제거
- 폐/근육 기반 구 케미 설명 제거 또는 `향후 확장`으로 이동
- 결과 화면 빌드 이름도 `mainClass + 획득 융합` 기준으로 생성
- 생활 보관함도 현재 3장 생활 카드 중심으로 정리

### P1. 전투 피드백 개선

- 심장 근접 타격 방향과 주먹/베기 애니메이션을 더 명확하게 표현
- 4타째 직전 콤보 카운터 또는 리듬 표시
- 뇌 코어마다 발사 순간 recoil/flash 추가
- 연쇄탄 경로를 선 또는 짧은 번개로 표시
- 간 독 지대 중첩과 3킬 폭발 게이지를 지대 위에 표시
- 독 중첩 수를 적 위에 읽기 쉬운 아이콘으로 표시
- 융합 카드 획득 시 짧은 전용 연출 추가
- 각성 순간 캐릭터 클로즈업과 직업명 연출 강화

### P1. HUD 개선

- 현재 획득한 직업 카드와 레벨을 게임 중 확인할 수 있는 패널
- 보호막 수치 표시
- 부활 가능 여부 표시
- 재생 인자 `현재 킬 / 20` 표시
- 심장 콤보 `1/4` 표시
- 간 지대 중첩 및 폭발 충전 상태 표시
- 모바일에서 HUD가 전투 시야를 가리지 않는지 실제 기기 확인

### P1. Game Lab 분석 이벤트 강화

기존 실행/시간 이벤트 외에 아래를 추가한다.

- `organ_level_up`
- `class_awakening_offered`
- `class_awakened`
- `awakening_deferred`
- `card_acquired`
- `fusion_condition_met`
- `fusion_acquired`
- `player_revived`
- `shield_created`
- `build_snapshot` — 30초 또는 스테이지 종료 시

최종적으로 Game Lab에서 확인할 지표:

- 직업별 선택률
- 각성 제안 대비 수락률
- 각성까지 걸린 시간
- 카드별 선택률
- 융합 도달률
- 직업/융합별 30초·60초 생존율
- 직업별 중앙값 플레이타임
- 사망 시 빌드 구성
- 최종 보스 도달률

### P2. 코드 구조 리팩터링

`app/organ-game.tsx`가 너무 커졌고 이전 시스템 코드도 함께 남아 있다.

권장 분리:

```text
app/game/
  cards/
    definitions.ts
    selection.ts
    effects.ts
  combat/
    heart.ts
    brain.ts
    liver.ts
    damage.ts
  rendering/
    player.ts
    enemies.ts
    effects.ts
  telemetry.ts
  state.ts
```

정리 대상:

- 구 `CHEMISTRY`, `LIFE`, `AUG`, `BASIC`, `BUILDS` 중 신규 시스템에서 사용하지 않는 데이터
- 이전 5장기 중심 능력과 신규 3직업 능력이 동시에 적용되어 예상치 못한 중첩을 만들 수 있는 부분
- 카드 트리거를 현재의 개별 `cardLevel(...)` 분기에서 공통 이벤트 훅/모디파이어 구조로 점진적으로 이전

리팩터링은 기능 검증과 밸런싱 이후에 하는 편이 안전하다.

### P2. 테스트 자동화

- 카드 풀에 항상 27장 정의가 존재하는지
- 직업 카드가 각성 전에 나오지 않는지
- 클래스와 맞지 않는 직업 카드가 나오지 않는지
- 조건 달성한 융합이 다음 선택에 포함되는지
- 1회성 카드가 재등장하지 않는지
- 직업 카드는 Lv.3 이후 재등장하지 않는지
- 부활은 한 번만 발생하는지
- 모바일 landscape에서 카드 세 장이 viewport를 벗어나지 않는지
- Game Lab postMessage payload 테스트

현재 `npm test`는 스타터의 rendered HTML 테스트에 가깝기 때문에 게임 로직 단위 테스트를 별도로 추가해야 한다.

### P2. 저장소와 문서 정리

- 루트 `README.md`는 아직 vinext starter 설명이다. 실제 게임 README로 교체
- 카드 원본 명세를 `docs/card-system.md`로 보존
- V1 에셋 보관 정책 결정
- `tmp/`, `.next/`, `tsconfig.tsbuildinfo` Git 제외 확인
- Cloudflare worker 타입을 쓸 계획이 없다면 관련 starter 파일 제거
- 쓸 계획이면 `@cloudflare/workers-types` 또는 해당 환경 타입 설정으로 `tsc` 오류 해결

## 8. 구현 시 지켜야 할 제품 원칙

1. 카드를 먹었을 때 화면이나 플레이 판단이 달라져야 한다.
2. 단순 `공격력 +N` 카드로 되돌아가지 않는다.
3. 장기는 스탯이 아니라 빌드 언어다.
4. 세 직업은 이펙트 색만 다른 원거리 캐릭터가 되어서는 안 된다.
5. 모바일에서는 이동 하나를 기본 조작으로 유지한다.
6. 신규 장기를 늘리기 전에 심장/뇌/간 세 직업의 차이를 완성한다.
7. 대회 제출에 필요한 재미와 증거에 직접 기여하지 않는 기능은 만들지 않는다.
8. 회원가입, 소셜, 랭킹, 복잡한 관리자 기능은 필요 없다.

## 9. Claude Code에 바로 전달할 시작 프롬프트

```text
이 저장소는 NHN NAN 2026 대회용 탑다운 생존 로그라이크 ‘장기 프로젝트’다.

먼저 CLAUDE_HANDOFF.md를 끝까지 읽고, git status와 현재 diff를 확인해라. 최신 기능은 Vercel 프로덕션에는 배포됐지만 아직 Git에 커밋되지 않았으므로 기존 변경을 절대 되돌리지 마라.

첫 작업은 다음 순서로 진행해라.

1. 현재 working tree의 신규 카드 시스템과 에셋을 검토한다.
2. tmp/와 tsconfig.tsbuildinfo를 제외하고 현재 구현을 안전하게 커밋한다.
3. 심장/뇌/간과 6개 융합을 빠르게 검증할 debug mode를 추가한다.
4. 각 전투 유형을 실제 브라우저에서 플레이 테스트한다.
5. 발견한 런타임 오류와 명세 불일치를 수정한다.
6. 표준 난이도의 첫 각성 시점과 직업 간 DPS/생존 밸런스를 조정한다.
7. npm run build 후 기존 Vercel project-organ 프로젝트에 production 배포한다.

핵심 제품 원칙은 ‘장기는 스탯이 아니라 플레이 스타일을 결정하는 빌드 언어’다. 새 기능을 추가하기보다 현재 세 직업이 이동 판단부터 다르게 느껴지도록 완성하는 것을 우선해라.
```

## 10. 완료 기준

다음 조건을 만족하면 대회용 핵심 시스템 1차 완성으로 본다.

- 세 직업 모두 각성 전후 전투 방식 차이가 즉시 느껴짐
- 여섯 융합 모두 실제 전투에서 확인 가능
- 각 직업으로 1회 이상 최종 보스까지 완주 가능
- 모바일 실제 기기에서 이동, 액션, 카드 선택이 문제없음
- 직업/카드/융합 선택 이벤트가 Game Lab DB에 기록됨
- 직업별 선택률, 각성 시간, 생존 시간, 사망 빌드를 대시보드에서 비교 가능
- GitHub main, Vercel production, 로컬 소스가 같은 상태
